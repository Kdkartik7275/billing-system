import 'package:flutter/material.dart';

class ReportColors {
  ReportColors._();

  // ===========================================================================
  // BACKGROUNDS
  // ===========================================================================

  static const background = Color(0xffF7FAFC);

  static const card = Colors.white;

  static const surface = Color(0xffFFFFFF);

  static const surfaceSoft = Color(0xffF9FBFD);

  static const surfaceMuted = Color(0xffF3F6F9);

  // ===========================================================================
  // PRIMARY
  // ===========================================================================

  static const primary = Color(0xff2962FF);

  static const primaryDark = Color(0xff1D4ED8);

  static const primaryLight = Color(0xffEAF2FF);

  static const primarySoft = Color(0xffF3F7FF);

  // ===========================================================================
  // TEXT
  // ===========================================================================

  static const text = Color(0xff172B4D);

  static const secondaryText = Color(0xff6B7A90);

  static const mutedText = Color(0xff8795A8);

  static const disabledText = Color(0xffAAB5C4);

  static const whiteText = Colors.white;

  // Alias used by some widgets.
  static const muted = mutedText;

  // ===========================================================================
  // BORDERS / DIVIDERS
  // ===========================================================================

  static const border = Color(0xffE5EDF5);

  static const borderLight = Color(0xffEEF2F6);

  static const divider = Color(0xffE8EDF2);

  static const dividerDark = Color(0xffD9E1EA);

  // ===========================================================================
  // SUCCESS / CASH BALANCED
  // ===========================================================================

  static const green = Color(0xff12B76A);

  static const greenDark = Color(0xff087443);

  static const greenLight = Color(0xffE8F8F0);

  static const greenSoft = Color(0xffF2FBF6);

  // ===========================================================================
  // WARNING / CASH OVER
  // ===========================================================================

  static const orange = Color(0xffEF6C00);

  static const orangeDark = Color(0xffB54708);

  static const orangeLight = Color(0xfffff2e8);

  static const orangeSoft = Color(0xfffff9f4);

  // ===========================================================================
  // ERROR / CASH SHORT
  // ===========================================================================

  static const red = Color(0xffE53935);

  static const redDark = Color(0xffB42318);

  static const redLight = Color(0xffffeeee);

  static const redSoft = Color(0xfffff6f6);

  // ===========================================================================
  // PURPLE
  // ===========================================================================

  static const purple = Color(0xff6941C6);

  static const purpleDark = Color(0xff53389E);

  static const purpleLight = Color(0xffF4F0FF);

  static const purpleSoft = Color(0xffFAF8FF);

  // ===========================================================================
  // INFO
  // ===========================================================================

  static const info = Color(0xff0284C7);

  static const infoDark = Color(0xff0369A1);

  static const infoLight = Color(0xffE0F2FE);

  static const infoSoft = Color(0xffF0F9FF);

  // ===========================================================================
  // NEUTRAL / BLACK
  // ===========================================================================

  static const black = Color(0xff111827);

  static const blackSoft = Color(0xff374151);

  static const grey = Color(0xff6B7280);

  static const greyLight = Color(0xffF3F4F6);

  // ===========================================================================
  // CASH RECONCILIATION
  // ===========================================================================

  static const cash = Color(0xff12B76A);

  static const cashLight = Color(0xffE8F8F0);

  static const cashShort = Color(0xffE53935);

  static const cashShortLight = Color(0xffffeeee);

  static const cashOver = Color(0xff2962FF);

  static const cashOverLight = Color(0xffEAF2FF);

  // ===========================================================================
  // SHADOW
  // ===========================================================================

  static const shadow = Color(0x0D172B4D);

  static const shadowStrong = Color(0x1A172B4D);
}
